# 生活カレンダーアプリ 要件定義・実装計画

対象：特別支援学級（学年混在、複数レベルの見やすさに配慮）
運用形態：**先生の端末上で動作する1台完結型のアプリ**。児童もその画面を一緒に見て自分で確認する
方針：静的サイト（GitHub Pagesで配布）。組み込みの画像ライブラリなどアプリ本体はGit管理だが、**日々の行事データ・アップロード画像は端末のブラウザ内（localStorage / IndexedDB）に保存**し、Gitコミット不要で完結させる

---

## 1. 目的

学校行事や生活の流れ（栽培活動の経過、行事の準備〜本番など）を、児童が**画像中心**で直感的に見て理解できるようにする。「いつ」「なにがあるか」「今どの段階か」を視覚的に伝える。

## 2. 対象ユーザー

- 児童（学年混在。文字だけに頼らず、アイコン・写真で理解できることを最優先。先生端末の画面を一緒に見て自分で確認する）
- 担任（先生端末上でアプリを使い、行事の追加・画像のアップロードを行う。日常操作はアプリ内で完結し、Git操作は不要）

## 3. 機能要件

### 3.1 必須機能

| 機能 | 内容 |
|---|---|
| 月間カレンダー表示 | 月単位のグリッド。行事がある日にアイコン/サムネイル表示 |
| 帯表示（タイムライン） | 月ごとに区切らず、1年間（設定期間）を通した**1本の長い横スクロール帯**で表示。行事ごとに帯を縦に並べ、各帯の中に複数日にまたがる段階（例：ポップコーン収穫→乾燥→調理）を配置。「今日はここ」マーカー付き、初期表示は今日の位置にスクロールした状態 |
| 行事の画像表示 | 各行事・各段階に写真/イラストを紐付けて表示 |
| 画像ライブラリ（組み込み） | よく使う行事・活動の画像をあらかじめ用意し、担任が選ぶだけで使えるようにする（アプリ本体の一部としてGitで配布） |
| 画像追加（担任用） | アプリ内のアップロードUI（ファイル選択）から画像を追加し、**IndexedDBに保存**。Gitコミットは不要 |
| 行事データ管理 | アプリ内の入力フォームで行事を追加・編集し、**localStorageに保存**。初回起動時は`events.json`のサンプルデータを初期値として読み込む |
| 画像選択パレット | 組み込みライブラリとアップロード済み画像をカテゴリ別に一覧表示し、行事編集フォームからサムネイルを選んで紐付けられるようにする |
| 帯表示の印刷 | 1本の長い帯をA3サイズ数枚に分割して印刷し、印刷後に切って貼り合わせて1本の長い帯として使えるようにする |
| バックアップ（書き出し/読み込み） | 行事データ・アップロード画像をファイルに書き出し・読み込みできるようにする（ブラウザのキャッシュ削除等によるデータ消失に備える） |

### 3.2 任意機能（おまけ）

| 機能 | 内容 |
|---|---|
| 1日のスケジュール表示 | 時間割形式で「いま何をする時間か」をアイコン+時刻で表示 |

## 4. 非機能要件

- 視覚優先：文字は最小限、色分けとアイコン/写真で判断できること
- Chromebook・タブレットでの利用を想定したレスポンシブ対応
- GitHub Pagesでの静的配信（バックエンド不要）。日々のデータ操作は端末内で完結し、外部サーバーへの送信は行わない
- 担任がアプリ内の操作だけでデータ・画像を扱えるシンプルなUI

### 4.1 児童の個人情報保護に関する方針

- 行事データ・アップロード画像は**先生端末のブラウザ内（localStorage / IndexedDB）にのみ保存**され、外部サーバーやGitHubへ自動送信されることはない
- **他の利用者からアクセス可能になる「共有カタログ」機能は実装しない**（そもそも端末内で完結するため、児童が写った画像が意図せず外部公開される経路自体が存在しない設計とする）
- 組み込みライブラリ（`images/library/`、Git配布のイラスト・汎用素材）と、担任が端末内でアップロードする画像（IndexedDB保存、児童の写真等を含みうる）は明確に区別する
- バックアップ機能でファイル書き出しを行った場合、そのファイル自体の保管（USBメモリ・共有フォルダ等）は担任の運用に委ねる旨を明記しておく

---

## 5. リポジトリ構成（案）

```
seikatsu-calendar/
├── index.html          # 月表示・帯表示 切替のメイン画面
├── day.html             # 1日のスケジュール（任意機能）
├── css/
│   └── style.css        # 共通デザイン（配色・フォント・大ボタン等）
├── js/
│   ├── storage.js        # localStorage（行事データ）／IndexedDB（画像）の読み書きラッパー
│   ├── backup.js         # データ・画像のエクスポート/インポート（バックアップ）
│   ├── calendar.js       # 月表示ロジック
│   ├── timeline.js       # 帯表示ロジック（期間設定・スクロール）
│   ├── timelinePrint.js  # 帯表示のA3分割印刷ロジック（のりしろ・裁断線・シート番号）
│   └── daySchedule.js    # 1日表示ロジック（任意機能）
├── data/
│   └── events.seed.json  # 初回起動時に読み込む行事データのサンプル（以降はlocalStorageが正）
└── images/
    ├── library/           # 組み込み画像ライブラリ（カテゴリ別、Git配布・アプリ本体の一部）
    │   ├── gyouji/         # 行事（運動会・学習発表会・卒業式 等）
    │   ├── nouji/          # 栽培・収穫・調理
    │   ├── seikatsu/       # 給食・そうじ・朝の会 等の日常
    │   └── kisetsu/        # 季節（桜・梅雨・夏休み・雪 等）
    └── manifest.json       # ライブラリ画像の一覧（画像選択パレット用）
```

担任がアップロードする画像は、上記のファイル構成には含まれず、実行時にIndexedDBへ直接保存される（Gitには乗らない、端末内データ）。

## 6. データ設計（events）

2種類のイベントタイプを用意し、月表示・帯表示の両方から参照する。初回起動時は`data/events.seed.json`を読み込み、以降はlocalStorageに保存された行事データを正とする（画像は別途IndexedDBに保存し、行事データ側は画像IDで参照する）。

```json
{
  "events": [
    {
      "id": "popcorn-2026",
      "title": "ポップコーンの世話",
      "category": "nouji",
      "type": "stage",
      "stages": [
        { "date": "2026-09-10", "label": "しゅうかく", "imageId": "harvest_01" },
        { "date": "2026-09-20", "label": "かんそう",   "imageId": "dry_01" },
        { "date": "2026-10-10", "label": "ちょうり",   "imageId": "cook_01" }
      ]
    },
    {
      "id": "gakugeppyoukai-2026",
      "title": "がくげいはっぴょうかい",
      "category": "gyouji",
      "type": "stage",
      "stages": [
        { "date": "2026-11-01", "label": "れんしゅうかいし", "imageId": "practice_01" },
        { "date": "2026-11-20", "label": "リハーサル",       "imageId": "rehearsal_01" },
        { "date": "2026-11-25", "label": "ほんばん",         "imageId": "event_01" }
      ]
    }
  ]
}
```

- `imageId` は「組み込みライブラリの画像パス」または「IndexedDBに保存された担任アップロード画像のID」のどちらかを指す（storage.js側でどちらか判定して解決する）
- `type: "stage"` の行事は帯表示で「複数段階の帯」として描画。月表示では各stage.dateにアイコンを表示。
- 単発行事（遠足など）はstagesを1つだけ持たせれば同じ構造で扱える。

### 6.1 データ保存方式（localStorage / IndexedDB）

- **行事データ**（上記JSON相当）：`localStorage`に保存。サイズが小さく同期処理で済むため
- **画像データ**（担任アップロード分）：`IndexedDB`に保存。localStorageより容量が大きく、Blobをそのまま扱えるため写真の保存に向く
- **組み込みライブラリ画像**：アプリ本体としてGit配布（`images/library/`）。担任が追加するものではないため対象外
- **バックアップ機能**：行事データ（localStorage）＋アップロード画像（IndexedDB）をまとめて1つのファイルに書き出し／読み込みできるようにし、ブラウザのキャッシュ削除等によるデータ消失に備える

### 6.2 帯表示（タイムライン）の見せ方

- 月単位で分割しない。**年間（または運用期間）を通した1本の長い帯**を横方向に配置し、ユーザーが横スクロールして時間軸を移動する
- 行事は縦に複数レーンとして並べ、各レーンの中で `stages` を時系列の帯として描画する（例：ポップコーンのレーンに「収穫」「乾燥」「調理」の3区画が連続して並ぶ）
- 現在日には縦線などの「今日」マーカーを表示し、初期表示時は自動的に今日の位置までスクロールしておく
- 帯がカバーする期間は**デフォルトで学期単位**とするが、担任が期間を自由に変更できる設定項目を用意する（年間通し／学期／任意の開始日〜終了日 など）

### 6.3 帯表示の印刷機能

- 画面上の1本の長い帯を、**A3サイズ数枚（目安3枚程度）に分割して印刷**できるようにする
- 印刷後に児童・担任が**切って貼り合わせ、1本の長い帯として掲示できる**ことを前提にレイアウトする
- 各シートに以下を入れて、切り貼りの作業をやりやすくする
  - シート番号（例：1/3、2/3、3/3）と貼り合わせ順の矢印/ガイド
  - 隣のシートと同じ日付を数cm分だけ重ねて印刷する「のりしろ」領域（切る位置の目印線付き）
- 印刷対象の期間は画面表示中の期間（6.2のカバー範囲設定）と連動させる。印刷専用のCSS（`@media print`）でシート分割・のりしろ・裁断線を制御する

## 7. 画像ライブラリ設計

視覚支援教材（ドロップレット・プロジェクトの「ドロップス」、コバリテ等）や学校向けイラスト素材サイトでよく使われるカテゴリ構成を参考に、以下のカテゴリ体系とする。既存素材そのものは著作権・利用規約の制約があるため同梱しない。オリジナル制作または生成、もしくは利用規約の範囲内で担任が個別に用意する画像を前提とする。

- カテゴリ：
  - `gyouji`（行事）：入学式・卒業式・運動会・学習発表会・遠足・修学旅行・避難訓練・参観日 等
  - `nouji`（栽培・調理）：種まき・水やり・観察・収穫・乾燥・調理 等
  - `seikatsu`（日常生活動作）：朝の会・帰りの会・給食・そうじ・着替え・手洗い・歯みがき・トイレ 等
  - `basho`（場所）：教室・体育館・保健室・図書室・校庭・トイレ 等（1日のスケジュール表示でも使用）
  - `kimochi`（気持ち）：うれしい・かなしい・おこった・こまった・つかれた 等（任意機能だが視覚支援では定番）
  - `kisetsu`（季節）：桜・梅雨・夏休み・冬休み・雪・お正月・クリスマス 等
  - `kigou`（進捗・記号）：やじるし・○✕・チェックマーク・今日マーカー用アイコン 等（帯表示・スケジュール表示の補助に使用）
- 命名規則：`カテゴリ_内容_連番.png`（例：`harvest_popcorn_01.png`）
- `images/manifest.json` に画像パスと日本語ラベルを一覧化し、画像選択パレット（組み込み分）の表示に使う

---

## 8. 実装タスク分割（並行作業用）

**最優先で確定させるもの：行事データのスキーマ＋storage.jsのAPI形（上記6章）**。これが全モジュールの共通契約になるため、他の作業と並行させず最初に固める。

スキーマ確定後は、以下を**独立したファイル境界**で並行して進められる。

| ストリーム | 担当範囲 | 主な成果物 | 依存 |
|---|---|---|---|
| A. データ基盤 | events.seed.json作成、localStorage/IndexedDB読み書きラッパー、バックアップ（書き出し/読み込み） | `data/events.seed.json`, `js/storage.js`, `js/backup.js` | スキーマ確定後すぐ着手 |
| B. 月表示 | カレンダーグリッドUI、日クリックで詳細表示 | `js/calendar.js` | storage.jsのAPI形（関数シグネチャ）だけ先に決めれば並行可 |
| C. 帯表示 | 年間通しの1本の横スクロール帯UI、行事ごとのレーン分け、段階の帯描画、今日マーカー・初期スクロール位置、カバー期間の設定機能 | `js/timeline.js` | 同上 |
| C2. 帯表示の印刷 | A3分割印刷ロジック、のりしろ・裁断線・シート番号、印刷用CSS | `js/timelinePrint.js` | Cのレーン・期間データ構造が固まってから着手（Cと同時並行でも設計だけ先に握れば可） |
| D. 1日表示（任意） | 時間割UI | `js/daySchedule.js`, `day.html` | 独立して進行可 |
| E. 画像ライブラリ＋選択パレット | カテゴリ別画像収集/生成（7章の7カテゴリ）、manifest.json作成、画像選択パレットUI（アップロードUIも含む） | `images/library/**`, `images/manifest.json`, パレットUIコンポーネント | 独立して進行可（アップロードUI部分はstorage.jsのAPI形が決まり次第着手） |
| F. 共通デザイン | 配色・フォント・大ボタン・共通ヘッダー | `css/style.css` | 独立して進行可（他ストリームがクラス名を合わせて使う） |
| G. 統合 | index.htmlでの画面切替、全体結合、GitHub Pages設定 | `index.html`, デプロイ設定 | B〜Fが揃ってから |

### 進行順序の目安

1. 行事データのスキーマ確定＋storage.jsの関数インターフェース決定（例：`getEvents()`, `saveEvent(event)`, `saveImage(file) → imageId`, `getImage(imageId)`）
2. B・C・D・E・Fを並行実施
3. Gで統合、動作確認、GitHub Pagesへデプロイ

各ストリームが触るファイルが重ならないため、codexへ同時に渡してもコンフリクトが起きにくい構成になっている。

---

## 9. 未確定・要検討事項

- リポジトリ名（既存の `sonohoka` / `keisan2` とは別に新規リポジトリを想定）
- 印刷ののりしろ幅・A3分割の枚数（内容量によって2枚〜4枚程度に変動する可能性あり、実装時に調整）
- バックアップ機能は「入れる」前提で反映済み。不要であれば後で外せます
