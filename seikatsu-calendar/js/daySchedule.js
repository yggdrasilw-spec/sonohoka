const key="seikatsu-calendar.day-schedule.v1", $=s=>document.querySelector(s); let items=JSON.parse(localStorage.getItem(key)||"[]");
function render(){items.sort((a,b)=>a.time.localeCompare(b.time));$("#schedule").innerHTML=items.length?items.map((item,index)=>`<article><time>${item.time}</time><b>${item.label}</b><button data-index="${index}">できた ✓</button></article>`).join(""):'<p>先生が予定を入れると、ここに出ます。</p>';document.querySelectorAll("#schedule button").forEach(button=>button.onclick=()=>{items.splice(+button.dataset.index,1);save()})}function save(){localStorage.setItem(key,JSON.stringify(items));render()}$("#today").textContent=new Date().toLocaleDateString("ja-JP",{year:"numeric",month:"long",day:"numeric",weekday:"long"});$("#add").onclick=()=>$("#dialog").showModal();$("#form").onsubmit=e=>{e.preventDefault();items.push({time:$("#time").value,label:$("#label").value});e.target.reset();$("#dialog").close();save()};render();
